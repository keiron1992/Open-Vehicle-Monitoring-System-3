name: Maxus-T90-Build
on: [push, workflow_dispatch]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Setup ESP-IDF
        run: |
          git clone --recursive --depth 1 --branch v4.4.6 https://github.com/espressif/esp-idf.git
          cd esp-idf
          ./install.sh esp32
          echo "IDF_PATH=$GITHUB_WORKSPACE/esp-idf" >> $GITHUB_ENV

      - name: Build Maxus T90 Firmware
        run: |
          # 1. Load the ESP-IDF environment
          . $GITHUB_WORKSPACE/esp-idf/export.sh
          
          # 2. Move into the actual firmware project directory
          cd vehicle/OVMS.V3
          
          # 3. Build with Component Manager DISABLED to avoid the v5.0.0 error
          rm -rf build
          idf.py -DIDF_COMPONENT_MANAGER=0 -DCMAKE_POLICY_VERSION_MINIMUM=3.5 build
          
      - name: Upload Finished Firmware
        uses: actions/upload-artifact@v4
        with:
          name: Maxus-T90-Firmware
          path: vehicle/OVMS.V3/build/ovms3.bin
